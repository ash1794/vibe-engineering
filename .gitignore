.DS_Store
*.swp
*.swo
*~
.env
node_modules/

# Prevent recursive self-nesting in Claude Code plugin cache (see #1).
# Root-anchored so it doesn't clobber plugins/vibe-engineering/.
/vibe-engineering/
